<div className="ml-[144px] mt-[80px] h-[420px] w-[570px]">
  <div className="flex flex-col space-y-4">
    {userInfo.map((item) => (
      <div
        key={item.id}
        className="flex w-[570px] text-[32px] leading-[44px]"
      >
        <span className="text-gray-500 w-[200px]">{item.label}</span>
        <span className="ml-[200px]">{item.value}</span>
      </div>
    ))}
  </div>
</div>